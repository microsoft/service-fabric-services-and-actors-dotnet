<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  <PropertyGroup Condition="$([MSBuild]::IsOSPlatform('Windows'))">
    <_NetFramework>;net472</_NetFramework>
  </PropertyGroup>
</Project>
